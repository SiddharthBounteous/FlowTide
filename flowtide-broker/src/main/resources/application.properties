spring.application.name=flowtide-broker
server.port=0
server.host=localhost

kafka.topic.default-partitions=3
kafka.topic.default-replication-factor=2

kafka.backpressure.max-total-events=5000000
kafka.backpressure.max-total-bytes=1073741824
kafka.backpressure.retry-after-seconds=5

kafka.cluster.heartbeat-interval-ms=5000
kafka.cluster.node-timeout-ms=15000

kafka.retention.check-interval-ms=60000

# Replication — how often follower brokers poll the leader for new events (catch-up phase)
kafka.replication.poll-interval-ms=1000
# ISR — min replicas that must ACK before write is confirmed (1 = leader only, 2 = leader + 1 follower)
kafka.replication.min-isr=1
# ISR — max ms to wait for follower ACK before removing it from ISR
kafka.replication.ack-timeout-ms=2000

kafka.offsets.persist-path=data/offsets.json

management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.metrics.tags.application=${spring.application.name}

logging.level.com.bounteous=INFO
logging.level.AUDIT=INFO

# Eureka client — registers this broker with the service registry
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
# Each broker instance needs a unique ID so they don't overwrite each other in Eureka.
# Without this, all instances with server.port=0 would share the same instance-id
# (hostname:flowtide-broker:0) and only the last one to register would be visible.
eureka.instance.instance-id=${spring.application.name}:${random.uuid}
# Tell Eureka to re-read the actual bound port after Tomcat starts (important for server.port=0)
eureka.instance.non-secure-port-enabled=true
